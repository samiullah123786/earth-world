// Compatibility import for older Kernel modules and cached generated types.
// Compatibility export for callers while the authoritative source is the
// generated Tiled CollisionLayer.
export { H, ROWS, TILED_FOUNDING_FORMAT, W, walkable } from './tiledFounding';
